## Future-Proofing Cost Reductions — Implementation Plan\n\n### Status check (what's already done, won't redo)\n\nWhile exploring I confirmed three items in `.lovable/plan.md` are already in place:\n\n- `useRealtimeSync` debounce default is **already 1500 ms** (not 250 ms as plan.md claimed).\n- Per-key dedupe window in `limsRealtimeDedupe.ts` is **already 2000 ms**.\n- `patient_results` is **NOT in the realtime publication** (confirmed by the in-file comment in `ResultsEntry.tsx` line 128 and by the Core memory rule). So the \