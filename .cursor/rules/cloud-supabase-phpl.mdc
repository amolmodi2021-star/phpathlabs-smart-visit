---
description: PHPL cloud Supabase test project migration context
alwaysApply: true
---

# Independent LIMS (no Lovable)

- This app is **PH PathLabs LIMS**, hosted at `https://lims.phpathlabs.com`.
- Do **not** add Lovable: no `lovable-tagger`, no `lovable.dev` / `lovable.app` / `lovableproject.com` URLs, no `ai.gateway.lovable.dev`, no `LOVABLE_API_KEY`, no `.lovable/` project sync.
- Do not send users to the Lovable editor. Deploy from this GitHub repo + Supabase cloud only.
- Prescription scan uses `GEMINI_API_KEY` or `OPENAI_API_KEY` (Supabase Edge secrets), never Lovable AI.

# Cloud Supabase (test) — PHPL

- Organization: **PH PathLabs Database** (FREE)
- New test project name: **PHPL**
- Project URL: `https://gqpqnfvihjjkmbcdzate.supabase.co`
- Project ref: **`gqpqnfvihjjkmbcdzate`** (CLI linked; ACTIVE_HEALTHY; Postgres 17 Mumbai)
- App `.env` now points at **PHPL cloud** (backup: `.env.before-cloud-shift`)
- Edge functions deployed; secrets in `supabase/.env.cloud-phpl-secrets` (gitignored locally — do not commit)
- Local data replica imported (`data-export-local-fresh`); cloud E2E audit passed with **no WhatsApp sends**
- Login for testing: `PHPATHLABS` / `admin123`
- Old/live cloud project ref (do not overwrite casually): `rpbkilhzulaugzrlatts`
- Local Docker still available at `http://127.0.0.1:54421` via `.env.before-cloud-shift`
- Region: **South Asia (Mumbai)**
- Security at create: Data API ON · Automatically expose new tables OFF · Automatic RLS ON
- Purpose: test first on this new empty project; do **not** wipe old production until user explicitly asks
- Old/live cloud project ref (do not overwrite casually): `rpbkilhzulaugzrlatts`
- Local Docker LIMS: `http://127.0.0.1:54421`
- When user is ready: wipe patient data / start fresh only on the project they name
- After create: `supabase link` → `db push` → deploy functions → set `DESKTOP_API_KEY` etc. → point `.env` at new project
- Never commit DB password, service_role key, or `.env` secrets

# Durable CLI auth (daily migrations — no interactive login)

- **Required secrets** (gitignored `supabase/.env.cli-auth`, template: `supabase/.env.cli-auth.example`):
  1. `SUPABASE_ACCESS_TOKEN` — Account → [Access Tokens](https://supabase.com/dashboard/account/tokens) (personal access token `sbp_…`). Current machine token name: **`cursor-phpl-daily`**, expires **2027-03-09**.
  2. `SUPABASE_DB_PASSWORD` — PHPL database password (Settings → Database, or reset via Management API). Needed for `db push` / `link`, separate from the access token.
- Do **not** rely on interactive `npx supabase login` (browser session expires). Prefer:
  - `powershell -File scripts/supabase-db-push.ps1` (loads `.env.cli-auth`, links, pushes), or
  - Load `.env.cli-auth` into the shell env, then `npx supabase db push --linked`.
- CLI may also be logged in via `npx supabase login --token <token> --name cursor-phpl-daily` (one-time; still keep `.env.cli-auth` as source of truth).
- Renew the access token before **2027-03-09** (or earlier if revoked). Never commit `.env.cli-auth`.
